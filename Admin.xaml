<Window x:Class="Rating_App.Admin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Rating_App"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Admin" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Background="Navy">
            <Button Name="btn_config"
                    Style="{DynamicResource button_tab}" Content="Cấu hình hệ thống"
                    Click="Btn_tab_Click"/>
            <Button 
                Name="btn_config_slide" 
               Style="{DynamicResource button_tab}" Content="Danh sách"
                    Click="Btn_tab_Click"/>
        </StackPanel>
        <Frame Grid.Column="1" Source="Config.xaml"/>
    </Grid>
</Window>
